{
  "name": "protos",
  "version": "1.0.0",
  "description": "This repo allows for a central location to define, and reuse blockless prototypes throughout different ecosytems and languages.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ts-proto": "^1.180.0"
  }
}
